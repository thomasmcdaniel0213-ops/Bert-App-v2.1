BERT v2.1 COMBINED TEST

HOME
- Full-display Bert logo
- Tournament Builder -> tournament.html (existing Bert v2 test build)
- Round Robin -> round-robin.html (latest v3 wizard)
- Ladder League -> Coming Soon
- MiLP -> Coming Soon

IMPORTANT
- Tournament Builder and Round Robin currently save local state independently in the same browser/device.
- JSON backup remains recommended for important events.
- This package is ready to upload as a GitHub Pages site after local testing.

UPDATE 1
- Bert logo in Tournament Builder and Round Robin returns to Home
- Round Robin opens at setup wizard on relaunch instead of trapping user inside prior event
- Edit Event Setup button
- New Event button
- Two-step Clear Event Contents confirmation
- True shared placement: identical Wins + Point Differential + Points For share the same rank/medal

UPDATE 2
- Round Robin quick-score auto-fill now applies to losing scores 0–9, so 11-0 pickles auto-fill correctly.
